

//this runs once
void setup(){
  //crate canvas
  size(1280, 800);
  
}

void draw(){
  if (keyPressed) {
    if (key == 'R' || key == 'R') {
      fill(0);
    }
  } else {
    fill(255);
  }
  rect(25, 25, 50, 50); //if key r is pressed the rectangle will go black I want the 
  //rectagle to spawn in a random position when key r is pressed also with a random 
  //and size
}

// respond to keypress 
void keyPressed() { 
   
}
  